import numpy as np
import environment.grid as grid
    
def map_generator(rows, columns, n_charging_stations = 1, n_agents = 2, p_obstacles = 0.3):
    MAP = np.zeros((rows, columns))
    
    
    
    for i in range(rows):
        for j in range(columns):
            ...
    
    
    return MAP
    


def main():
    ...


if __name__ == '__main__':
    main()
